# List all source files in this directory
#set(SOURCES
    #lambda_TileMap.cpp
    # Add more source files if needed
    #)

# Create a library target for the game logic component
#add_library(visual ${SOURCES})

